{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "composite": true, // only needed for project references
    "strict": true
  },
  "include": ["../api/src/interfaces", "validations", "types", "schemas", "index.ts", "../api/src/types/jwtPayload.ts"]
}
